abstract class WalletRepository {
  // Methods here will be for aggregated wallet logic or shared repository logic
  // if strictly necessary, but for now Transaction and Account repositories
  // handle their respective domains.
}
